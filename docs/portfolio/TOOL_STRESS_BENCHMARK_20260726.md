# Grounded Agent Benchmark

## 数据边界

- 当前 BOSS 实习岗位：50 个。
- 场景分布：`tool_stress`。
- 受控合成中文消息：337 条。
- 冷启动与增量回合：100 个。
- 岗位事实来自登录后的 BOSS 搜索与详情页快照；聊天为受控合成数据，不冒充真实 HR 对话。
- 标签由 10 类预定义招聘场景产生，用于验证路由、结构化输出和工具闭环，不等同于生产准确率。

## 结果

- 阶段命中率：100.00%
- 下一步行动命中率：100.00%
- 联合命中率：100.00%
- 冷启动：50 runs，0 次 LLM，0 tokens。
- 增量：50 runs，115 次 LLM，405576 tokens。
- 核心分析工具调用：31 次本地工具，32 次外部 MCP。
- 外部 MCP 分布：岗位详情 24 次，公司研究 8 次。

## Tool Observation

- `boss_fetch_job_detail`：24 次调用，24 次成功，新增证据 24 条，最终采用 24 条，平均耗时 7631.7 ms。
- `job_read`：11 次调用，11 次成功，新增证据 11 条，最终采用 11 条，平均耗时 13.0 ms。
- `memory_read`：8 次调用，8 次成功，新增证据 14 条，最终采用 11 条，平均耗时 12.8 ms。
- `research_company`：8 次调用，8 次成功，新增证据 28 条，最终采用 28 条，平均耗时 6415.4 ms。
- `skill_discover`：6 次调用，6 次成功，作为规划/规则上下文使用 6 次，平均耗时 0.0 ms。
- `skill_load`：6 次调用，6 次成功，作为规划/规则上下文使用 6 次，平均耗时 0.0 ms。

## 分场景质量

- `ambiguous_event`：6/6 接受，阶段与行动联合命中率 100.00%。
- `availability`：2/2 接受，阶段与行动联合命中率 100.00%。
- `employment_identity`：8/8 接受，阶段与行动联合命中率 100.00%。
- `interview_invite`：8/8 接受，阶段与行动联合命中率 100.00%。
- `job_condition_gap`：8/8 接受，阶段与行动联合命中率 100.00%。
- `material_sent`：3/3 接受，阶段与行动联合命中率 100.00%。
- `project_question`：8/8 接受，阶段与行动联合命中率 100.00%。
- `rejected`：1/1 接受，阶段与行动联合命中率 100.00%。
- `resume_request`：5/5 接受，阶段与行动联合命中率 100.00%。
- `silent_manual_followup`：1/1 接受，阶段与行动联合命中率 100.00%。

## Agent Loop 含义

- 首轮 LLM 读取本轮增量、机会状态和可用工具目录，自主判断是否存在信息缺口。
- 需要补证据时，LLM 发出 Tool Call；Runtime 执行本地工具或只读 MCP，并把 Observation 返回下一轮 LLM。
- 最终结构化输出经过 Self-check 与 CommitGate 后才能写入阶段、任务、草稿和摘要。
- 并非每条对话都调用 MCP：拒绝、材料请求或静默追问已有充分聊天证据，强制外查只会增加延迟和噪声。

## 可复现边界

- `.artifacts/grounded/` 保存本地冻结数据，不提交公开仓库。
- 结果受 BOSS 页面可用性、模型服务和网络状态影响。
- 该基准用于回归 Agent 路由、工具调用、证据闭环和故障降级，不能替代独立人工 Gold Set。
